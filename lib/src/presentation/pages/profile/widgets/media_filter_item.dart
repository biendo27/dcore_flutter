part of '../profile.dart';

class MediaFilterItem extends StatelessWidget {
  final MediaFilter mediaFilter;
  const MediaFilterItem({super.key, required this.mediaFilter});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
